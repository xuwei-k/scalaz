# Use Docker-based container (instead of OpenVZ)
sudo: false
os: osx
osx_image: xcode8.2
before_install:
  - brew install sbt
script:
  - java -version
  - sbt -J-Xmx3784m check-gen-type-classes test:compile
